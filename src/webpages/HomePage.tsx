import Header from "@/components/Header";

const HomePage = () => {
    return (
        <div>
            <Header />
            <main>
                <h1>Welcome to the HomePage</h1>
                <p>This is a simple page structure.</p>
            </main>
        </div>
    );
};

export default HomePage;
